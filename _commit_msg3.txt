[feat] 合规三件套 + Forge 自动下载 Java 修复 + 联机帮助文案核对 + 打包排除 easytier 备份

## 合规（离线登录地区限制）
- shadow_backend: 新增 isOfflineRestricted()——只有明确检测为 CN 才放行；未检测到地区（断网/屏蔽 GeoIP）与国外/港澳台同样受限（防转空子），正版登录后不限
- geoip_service: 检测失败 5 分钟自动重试（防止大陆用户网络抖动被永久误锁）；补 QTimer include
- HomePage/VersionSettingsOverlay 两个启动入口：离线模式下受限则右下角天蓝色 toast「进行正版登录前，离线登录无法使用，请先完成正版登录。」并阻止启动
- MultiplayerPage 底部新增白色合规声明（遵守所在国法律、联机功能仅用于多人联机）

## Forge 安装自动下载 Java 找不到 java.exe（内测报障）
- mod_loader_installer downloadAndExtractJava：Adoptium zip 顶层自带 jdk-17.0.x+x/ 目录 → 解压时剥离公共顶层目录，java.exe 正确落在 java_cache/{v}/bin/java.exe；新增 findJavaExeRecursive 兜底递归查找

## 联机帮助文案核对（对照实际实现逐条修正）
- 提权原因不再写「创建虚拟网络接口」（实际 --no-tun 无虚拟网卡）
- 房主流程对齐「等待MC服务器启动...」状态 + MC 可建房后启动
- NAT 四档对齐 calcConnectionDifficulty（OpenInternet→直连 / NoPAT·FullCone→简单 / Restricted·PortRestricted→中等 / 对称→困难）
- 端口说明：RPC 为房间码确定性生成（15880-65535）；MC 端口是房主本机端口（扫描检测，与房间码无关，原文案错误）
- 常见问题：发现中心超时 60s→30s（实锤代码值）；补中继不可达原因与 MC 服务器名「联机 MC服务器」

## 打包
- pack.ps1: 复制 bin/ 时排除 *.bak（easytier official-2.6.4 备份永不上架）

注意：file_downloader.cpp / mod_manager.cpp 为未完成 WIP，未随本批提交
