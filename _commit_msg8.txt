[fix] 快捷入口 Mod 按钮改 sidebar 同款内联可见性 + 删除光影包按钮

用户反馈：_isModded（readonly property 间接引用）在快捷入口不生效，
但 sidebar"Mod 管理"（内联 visible 表达式）能完美隐藏。借鉴 sidebar 写法修复。

修复：
- 三个按钮 visible: _isModded → 直接内联与 sidebar 完全相同的白名单判断表达式
  （lt ∈ Forge/Fabric/NeoForge/Quilt），不再经 readonly property 中转
- 删除"光影包"按钮：装了加载器也不一定有光影加载器（OptiFine/Iris 判定繁琐，
  OptiFine 常嵌入式安装），用户确认移除
- 移除临时 [扫描DIA] 诊断日志（shadow_backend.cpp）

验证：eval 实测 26.2（loaderType=原版）按钮隐藏；编译通过（exe 22:48:57）
