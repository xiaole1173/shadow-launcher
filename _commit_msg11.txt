[fix] 用户数据导出修复（真凶：Q_INVOKABLE 返回具体类型指针 QML 无法处理）

## 完整诊断链（用户提供 QML 错误日志实锤）
- 日志：VersionSettingsOverlay.qml:1461: Error: Unknown method return type: UserDataBackend*
- 根因：backend.userDataBackend 是 Q_INVOKABLE 方法返回 UserDataBackend*（具体类型指针），
  QMetaType 未注册该类型 → QML 调用必报错（之前 eval 也报同样错误，与生产一致）。
  而 backend.multiplayer/modManager 能用的原因：它们是 Q_PROPERTY(QObject*) 属性（基类指针）。

## 修复
1. shadow_backend.h：userDataBackend + javaBackend 改为 Q_PROPERTY(QObject* xxx READ xxx CONSTANT)，
   getter 返回 QObject*（与声明一致，moc 需要）；补 include java_backend.h/userdata_backend.h
   （派生→基类转换需要完整类型）
2. QML 全部去括号：backend.userDataBackend() → backend.userDataBackend（属性），
   backend.javaBackend() → backend.javaBackend（DownloadPage）；共 12 处
3. MainWindow.qml：删除残留 item.confirmDialog = confirmDialog（VSO 已用本地确认对话框，
   该行启动时报 ReferenceError: confirmDialog is not defined）
4. 移除临时 testExport 诊断方法

## 验证（eval 实测）
- backend.userDataBackend 属性访问 OK（不再报 Unknown method return type）
- exportUserData 调用 → 8MB zip 完整生成（README.txt + game/saves/mods/options.txt 等）
- QML errors: 无
- C++ doExport 此前已改为版本目录根白名单收集（旧 game/ 子目录废弃）

exe 00:40:17
