[fix] 导出 toast 改用通用蓝色 ToastManager + doExport 失败分支直写日志

用户反馈：导出 toast 应使用通用组件蓝色 toast（非本地深色框）；
"无法导出zip文件"提示需定位。

改动：
1. VersionSettingsOverlay：导出开始/成功/失败 toast 全部改用 toastManager.show()
   （通用蓝色右下角 toast；失败 5s 时长）
2. userdata_backend.cpp：doExport 各失败分支 + 成功路径加 qCInfo(logApp) 直写日志
   （原 logMessage 信号转发链无人消费，日志文件里看不到 userdata 输出）
3. include ../utils/logger.h

验证：eval 实测英文/空格/中文路径导出全部成功（8MB zip，日志 [userdata] 导出完成）。
待用户实测定位失败分支。
