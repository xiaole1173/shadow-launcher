夸父超时策略：官方 12s×2 快速切镜像，修后期“卡死”

用户实测（23:39）：快下载完时剩余 3 个大文件（fastutil/icu4j/
lwjgl-vulkan）官方超时 30s×3=75s 才切镜像 → UI 进度长时间不动
= 用户感知“后期全部卡死”（实际下载在超时重试中）。

修复：
1. 超时 30s→12s（transferTimeout 是无数据超时，持续下载不受影响；
   官方 CDN 正常 1-2s 首字节，12s 无数据必是连接挂起）
2. 官方源只试 2 次（24s）就切镜像——实测 3 个大文件同时压官方
   必限流，切 BMCLAPI 秒下
3. 镜像源保持 15s（慢镜像容忍度稍高）
