Step B: pipeline integration — StepNode in rebuildSteps/updateStep/showStep/hideStep (P1)

- Add StepPipeline::clearSteps() to clear all steps from pipeline
- rebuildSteps(): create StepNode via pipeline->addStep() for each step
- updateStep(): update StepNode status/percentage/bytes alongside QVariantList
- showStep()/hideStep(): set StepNode::setHidden() alongside QVariantList flag
- updateCardFromSession(): fallback phase from ds->steps when pipeline phase is empty