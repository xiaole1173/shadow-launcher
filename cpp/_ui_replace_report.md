# 颜色替换报告

- 替换: **2** 种颜色 / **2** 处
- 保留(需人工): **18** 种 / 残留字面量 19 处

## 保留清单

- `#9911141c` × 2 — alpha色，非精确匹配
- `#c0101114` × 1 — alpha色，非精确匹配
- `#78787887` × 1 — alpha色，非精确匹配
- `#f01e1f26` × 1 — alpha色，非精确匹配
- `#00000000` × 1 — alpha色，非精确匹配
- `#99000000` × 1 — alpha色，非精确匹配
- `#252a1c10` × 1 — alpha色，非精确匹配
- `#1a2b1c00` × 1 — alpha色，非精确匹配
- `#804b3a00` × 1 — alpha色，非精确匹配
- `#664b3a00` × 1 — alpha色，非精确匹配
- `#f5d080` × 1 — 家族orange内最近 warning 距离 249 超阈值
- `#f0c060` × 1 — 家族orange内最近 warning 距离 186 超阈值
- `#60000000` × 1 — alpha色，非精确匹配
- `#5aa8c0` × 1 — 家族other内最近 info 距离 78 超阈值
- `#9cf888` × 1 — 家族green内最近 success 距离 155 超阈值
- `#abff7c` × 1 — 家族green内最近 success 距离 176 超阈值
- `#baff70` × 1 — 家族green内最近 success 距离 192 超阈值
- `#e8d870` × 1 — 家族orange内最近 warning 距离 235 超阈值