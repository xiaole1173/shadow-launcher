[fix] FileDialog.selectedFile 路径转本地：防御式兼容 QUrl/字符串两种形态

用户日志实锤：导出失败 "file:///C:/Users/蔡朝彬/Desktop/26.2_userdata.zip"——
path 仍带 file:/// 前缀，说明 QML 里 selectedFile 实际是字符串（非 QUrl 对象），
原 url.scheme === "file" 判断失效走了 toString()。

修复（VSO 导出 + InstallPage 导入两处 onAccepted）：
- 统一防御式：typeof 判断字符串/QUrl → toString → indexOf("file:///")===0 则 substring(8) 剥前缀
- 与 Qt 版本无关，两种形态都能转成 C:/... 本地路径

exe 00:53:40
